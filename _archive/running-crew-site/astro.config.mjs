// @ts-check
import { defineConfig } from 'astro/config';

// Phase 1 러닝크루 사이트 — 정적 사이트 (서버·DB 없음)
// 배포: Netlify/Vercel 무료 티어에 이 폴더를 연결하면 자동 빌드됩니다.
export default defineConfig({
  site: 'https://your-crew-domain.com', // ← 도메인 확정 후 교체
});
